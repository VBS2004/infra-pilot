# Same as `pip install -e '.[full]'`; kept for tooling that expects a requirements file.
#
# The indexer RUNS WITH ZERO DEPS (stdlib-only fallback parser + stdlib BM25),
# so you can try it immediately. Installing these upgrades it to the
# production backends described in the design plan.

# Primary HCL parser backend (replaces the stdlib fallback automatically)
tree-sitter>=0.21
tree-sitter-hcl>=1.1.0

# Lexical retrieval leg (replaces the bundled stdlib BM25 automatically)
rank-bm25>=0.2.2

# Dense leg (cosine over numpy vectors)
numpy>=1.26
